output.txt
node_modules
dist
.mastra
.env.development
.env
*.db
*.db-*

# Exception: Include pre-generated knowledge base
!knowledge-base.db
!knowledge-base.db-shm
!knowledge-base.db-wal
logs/

# Frontend build artifacts
frontend/dist
frontend/.vite
frontend/node_modules
public/

# Terraform local state and config
infra/.terraform/
infra/terraform.tfstate
infra/terraform.tfstate.*
infra/terraform.tfvars
.terraform.lock.hcl
infra/state-bootstrap/.terraform/
infra/state-bootstrap/terraform.tfstate
infra/state-bootstrap/terraform.tfstate.*
infra/state-bootstrap/terraform.tfvars
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run. Most
# .tfvars files are managed as part of configuration and so should be included in
# version control.
#
# example.tfvars
terraform.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
*tfplan*
